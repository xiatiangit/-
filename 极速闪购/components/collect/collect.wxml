<view class="collect">
    <view class="title">
        <view class="item {{item.active?'active':''}}" wx:for="{{val}}" wx:key="id"  bindtap="active" data-index="{{index}}">{{item.value}}</view>
    </view>
    <view class="content">
        <slot></slot>
    </view>
</view>
